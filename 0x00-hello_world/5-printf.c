#include <stdio.h>

/**
 * main - print the string when the printf
 *
 * Description: when called, the main function print
 * out the given text
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

